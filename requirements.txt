# Django and API Framework
django==5.0.1
django-ninja==1.1.0
django-cors-headers==4.3.1

# Database
psycopg2-binary==2.9.9  # For PostgreSQL (optional, SQLite is default)
dj-database-url==2.1.0  # Parse DATABASE_URL for PostgreSQL

# JWT Authentication
pyjwt==2.8.0
python-jose[cryptography]==3.3.0

# LangChain and LangGraph (compatible versions - langgraph 0.2.76 excludes langchain-core 0.3.0)
langchain==0.3.0
langchain-community==0.3.0
langchain-core==0.3.23  # Compatible with langgraph 0.2.76 (0.3.0 is excluded)
langchain-google-genai>=2.0.0,<3.0.0  # For Gemini - let it resolve compatible google-generativeai
langgraph==0.2.76  # Latest 0.2.x version

# ChromaDB for Vector Store (used by both Vanna and Document RAG)
chromadb==1.3.0

# Document Processing
pypdf2==3.0.1
python-docx==1.1.0
openpyxl==3.1.2  # For Excel files

# Embeddings
sentence-transformers==2.2.2  # Free embedding model alternative

# Utilities
python-dotenv==1.0.0
pydantic>=2.7.4,<3.0.0  # langchain 0.3.0 requires >=2.7.4, django-ninja requires >=2.0
pydantic-settings>=2.1.0,<3.0.0  # Must be compatible with pydantic version

# Testing (optional - comment out for production)
# pytest==7.4.3
# pytest-django==4.7.0
# pytest-asyncio==0.21.1
# pytest-timeout>=2.4.0
# httpx==0.25.2
# deepeval>=3.0.0

# Google Generative AI (for Gemini) - version resolved by langchain-google-genai
# google-generativeai will be auto-installed by langchain-google-genai

# Vanna AI for Text-to-SQL with RAG
vanna==0.7.0
pandas==2.0.3  # Required by Vanna

# Production Server
gunicorn==21.2.0

# Development (optional - comment out for production)
# black==23.11.0
# flake8==6.1.0

